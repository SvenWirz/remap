/**
 * Test fixtures for the nullness validation. This package is null-marked, so every type usage that is not annotated
 * with {@link org.jspecify.annotations.Nullable} is non-null.
 */
@NullMarked
package com.remondis.remap.nullness;

import org.jspecify.annotations.NullMarked;
